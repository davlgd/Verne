---
title: "Reference"
description: "Look-up pages for `verne.yaml`, the CLI, and the template engine."
---

The reference is the source of truth for the surface area Verne exposes:
every CLI flag, every configuration key, every template construct. It does
not narrate — it lists. Use the **Guides** for the *why*.
